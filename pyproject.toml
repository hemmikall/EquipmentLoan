#This file is used to package the EquipmentLoan plugin.
#
#- It was generated by the InvenTree Plugin Creator tool - version 1.13.4
#- Ref: https://github.com/inventree/plugin_creator

[build-system]
requires = ["setuptools", "twine", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "inventree-equipmentloan"
description = "Adds functionality to loan out equipment"
dynamic = ["version"]
authors = [
    { name = "Hermann K Bjornsson",email = "contact@ctrlelement.com" },
]
readme = "README.md"
license = "GPL-2.0"
keywords = ["inventree", "plugin"]
requires-python = ">=3.9"
dependencies = [
    # Enter your plugin library dependencies here
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Framework :: InvenTree",
]
[project.urls]
Homepage = "https://my-project-url.com"

[project.entry-points."inventree_plugins"]
EquipmentLoan = "equipmentloan.core:EquipmentLoan"

[tool.setuptools.packages.find]
where = [""]

[tool.setuptools.dynamic]
version = {attr = "equipmentloan.PLUGIN_VERSION"} 

[tool.ruff]
exclude = [
    ".git",
    "__pycache__",
    "test.py",
    "tests",
    "venv",
    "env",
    ".venv",
    ".env",
]

src=["equipmentloan"]
